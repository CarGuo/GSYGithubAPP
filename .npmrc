# GSYGithubApp .npmrc
# 工程化"依赖发布满 15 天才允许采用"的硬约束。
#
# - npm 11+ 原生支持以下两个等价字段（任意一个生效即可，npm 实际只识别其中一个，
#   按 npm 版本自动选择；老版本 npm 会自动忽略未识别字段，无副作用）：
#     * minimum-release-age
#     * cooldown
#   单位：分钟。15 天 = 21600 分钟。
#
# - 想临时绕过（紧急修复）：CI 或本地用：
#     npm install --minimum-release-age=0
#   或：
#     MIN_RELEASE_AGE_DAYS=0 node scripts/check-package-age.js
#
# - 配套自检脚本：scripts/check-package-age.js，兼容老 npm。
#
# - 详细策略见 harness/playbooks/upgrade-dependency.md 与 harness/playbooks/rn-0.85-upgrade-plan.md。

minimum-release-age=21600
cooldown=21600

# Node 引擎硬约束 —— 拒绝在 Node 18 等不兼容版本上 install
# 与 package.json#engines 配合（>=20.18.1 <21）
engine-strict=true

